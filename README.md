# jobscout-ai
Daily Python agent that pulls LinkedIn jobs from RecruitNepal, keeps only AI, LLM, agent, RAG, and automation roles, skips jobs already emailed, and sends one HTML Gmail briefing when something new appears. Runs every day at 06:00 UTC via GitHub Actions. Most days send nothing.
